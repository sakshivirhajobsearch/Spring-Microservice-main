eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: 'true'
    register-with-eureka: 'true'
  instance:
    hostname: localhost
spring:
  application:
    name: STUDENT-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://Senura:Mongodb8864@cluster0.0uinm.gcp.mongodb.net/test?retryWrites=true&w=majority
server:
  port: '8081'
